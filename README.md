# search-engine
Building the foundation of a search engine . This project includes a custom Python web crawler that systematically explores web pages, extracts links, and stores data for indexing and retrieval.
